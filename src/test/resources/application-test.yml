
spring:
  datasource:
    url: jdbc:tc:postgresql:13.2-alpine:///
    driverClassName:
    username: postgres
    password: password
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  shell:
    interactive:
      enabled: false
